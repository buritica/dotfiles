{{- $codespaces := env "CODESPACES" | not | not -}}
{{- $ci := env "CI" | not | not -}}

sourceDir = "{{ .chezmoi.sourceDir }}"

[data]
  name = "Juan Pablo Buritica"
  codespaces = {{ env "CODESPACES" | not | not }}
  email = "buritica@gmail.com"

[data.machine]
  hostname = "{{ .chezmoi.hostname }}"

# Capabilities — opt-in specialized workloads. Core is unconditional.
# ALL MACHINES CORE-ONLY for now. Capabilities are scaffolded in the Brewfile but
# not yet reviewed/tested — we enable them iteratively, per machine, after testing.
# To enable, add a hostname branch below (e.g. crowntail -> work=true).
{{- $caps := dict "work" false "make_music" false "play_games" false "media_server" false }}

[data.capabilities]
  work         = {{ $caps.work }}
  make_music   = {{ $caps.make_music }}
  play_games   = {{ $caps.play_games }}
  media_server = {{ $caps.media_server }}

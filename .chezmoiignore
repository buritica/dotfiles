# Documentation and repo files
LICENSE
README.md
CONTRIBUTING.md
CLAUDE.md
.cursorrules
CHANGELOG.md

# GitHub specific
.github/
pr_body.md

# Build and temp files
setup.sh
test.sh
.yamllint

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Git files
.gitignore

# State files (should persist outside chezmoi)
.my/.macos_configured

# Managed by ansible (machines repo) — not dotfiles.
# ansible writes ~/.tool-versions and runs mise install.
.tool-versions
.claude/settings.json

# id_halfmoon is halfmoon's local GitHub deploy key — only deploy its pubkey there.
{{ if ne .chezmoi.hostname "halfmoon" }}.ssh/id_halfmoon.pub{{ end }}
